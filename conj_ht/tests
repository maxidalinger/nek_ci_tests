[Tests]
  [conj_ht_cimode1]
    type = CSVDiff
    input = nek1.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 1'

    max_parallel = 20
    
    max_time = 10000 

    requirement = 'The system shall compute the error norms of velocity and temperature with respect to exact solution.'
    required_objects = 'NekRSProblem'
  []
  [conj_ht_cimode2]
    type = CSVDiff
    input = nek2.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 2'
  
    max_parallel = 20
    
    max_time = 10000 
  
    requirement = 'The system shall compute the error norms of velocity and temperature with respect to exact solution.'
    required_objects = 'NekRSProblem'
  []
  #[conj_ht_cimode3]
  #  type = CSVDiff
  #  input = nek3.i
  #  csvdiff = nek_out.csv
  #  cli_args = '--nekrs-cimode 3'
  #
  #  max_parallel = 20
  #
  # max_time = 10000
  #
  #  requirement = 'The system shall compute the error norms of velocity and temperature with respect to exact solution.'
  #  required_objects = 'NekRSProblem'
  #[]
[]
