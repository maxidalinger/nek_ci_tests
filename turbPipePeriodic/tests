[Tests]
  [turbPipePeriodic]
    type = CSVDiff
    input = nek.i
    csvdiff = nek_out.csv
    cli_args = '--nekrs-cimode 1'

    # mesh only contains 60 elements
    max_parallel = 20

    max_time = 1000 

    requirement = 'The system shall reproduce the wall stress for turbulent pipe flow.'
    required_objects = 'NekRSProblem'
  []
[]
